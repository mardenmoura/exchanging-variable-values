// Write the function that takes 2 numbers and returns the largest of them.

let higherValue = max (10,2)
console.log(higherValue)

function max(number1, number2){
    return number1 > number2 ? number1: number2
}